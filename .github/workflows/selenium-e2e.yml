name: Selenium End-to-End Test Suite

on:
  push:
    branches: [ main, master, dev ]
  pull_request:
    branches: [ main, master, dev ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18
        cache: 'npm'
        cache-dependency-path: safebank-web/package-lock.json

    - name: Install Root Dependencies
      working-directory: safebank-web
      run: npm install

    - name: Install React Application Dependencies
      working-directory: safebank-web
      run: npm run setup:app

    - name: Build React Application
      working-directory: safebank-web
      run: npm run build:app

    - name: Run E2E Tests on Headless Chrome
      working-directory: safebank-web
      run: npm run test:ci
      env:
        BROWSER: chrome
        HEADLESS: true
        ENVIRONMENT: CI_Pipeline

    - name: Upload Excel E2E Report
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: SafeBankAI_Test_Report
        path: safebank-web/excel/SafeBankAI_Test_Report.xlsx

    - name: Upload Mochawesome HTML Report
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: HTML-Report
        path: safebank-web/mochawesome-report/

    - name: Upload Error Screenshots & Failure Logs
      if: failure() || always()
      uses: actions/upload-artifact@v3
      with:
        name: Failure-Artifacts
        path: |
          safebank-web/screenshots/
          safebank-web/logs/
